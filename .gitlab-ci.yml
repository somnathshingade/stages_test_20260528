deploy-local:
  stage: deploy
  tags:
    - local-runner
  script:
    - echo "Deploying build output to C drive..."
    - powershell -Command "mkdir -Force 'C:/biginar_output'"
    - powershell -Command "Copy-Item -Path dist/* -Destination 'C:/biginar_output/' -Force"
  only:
    - main
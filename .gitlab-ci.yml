variables:
  VERSION:
    value: "15.0.0-SNAPSHOT"
    description: "build version of the project"


stages:
  - build
  - test
  - deploy

build_job:
  stage: build
  script:
    - |
       echo "==========================="
       echo "Hello from build_job"
       echo "==========================="
      
build_job1:
  stage: build          
  script:
    - |
      echo "==========================="
      echo "Hello from build_job1"
      echo "==========================="

build_job2:
  stage: build
  script:
    - |
      echo "==========================="
      echo "Hello from build_job2"
      echo "==========================="

test_job:
  stage: test
  script:
    - |
      echo "==========================="
      echo "Hello from test_job"
      echo "==========================="

DEV-ENV-deploy:
  stage: deploy
  trigger:
    project: in/it/plm/eplan/somnath/templates
    branch: templates_Downstream
    strategy: depend
    forward:
      yaml_variables: true
      pipeline_variables: true
  variables:
    ENV_HOSTNAME: "dev.somnath.com"
    Artifact_version: $VERSION
 
stages:
  - build
  - test
  - deploy

build_job:
  stage: build
  script:
    - |
       echo "==========================="
       echo "Hello from build_job"
       echo "==========================="
      
build_job1:
  stage: build          
  script:
    - |
      echo "==========================="
      echo "Hello from build_job1"
      echo "==========================="

build_job2:
  stage: build
  script:
    - |
      echo "==========================="
      echo "Hello from build_job2"
      echo "==========================="

test_job:
  stage: test
  script:
    - |
      echo "==========================="
      echo "Hello from test_job"
      echo "==========================="

DEV-ENV-deploy:
  stage: deploy
  trigger:
    project: in/it/plm/eplan/somnath/templates/-/blob
    branch: templates_Downstream
    strategy: depend
    forward:
      yaml_variables: true
      pipeline_variables: true
  variables:
    ENV_HOSTNAME: "dev.devopswithprashant.com"
       
QA-ENV-deploy:
  stage: deploy
  trigger:
    project: in/it/plm/eplan/somnath/templates/-/blob
    branch: templates_Downstream
    strategy: depend
    forward:
      yaml_variables: true
      pipeline_variables: true
  variables:
    ENV_HOSTNAME: "QA.devopswithprashant.com"  

PROD-ENV-deploy:
  stage: deploy
  trigger:
    project: in/it/plm/eplan/somnath/templates/-/blob
    branch: templates_Downstream
    strategy: depend
    forward:
      yaml_variables: true
      pipeline_variables: true
  variables:
    ENV_HOSTNAME: "PROD.devopswithprashant.com"

STAGE-ENV-deploy:
  stage: deploy
  trigger:
    project: in/it/plm/eplan/somnath/templates/-/blob
    branch: templates_Downstream
    strategy: depend
    forward:
      yaml_variables: true
      pipeline_variables: true
  variables:
    ENV_HOSTNAME: "STAGE.devopswithprashant.com"
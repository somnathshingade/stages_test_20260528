.deploy_job1:          # add . and ignore the job
  stage: deploy
  script:
    - |
        echo "=========================================================="
        echo "Deploying the application... $NODE_ENV & $APP_VERSION"
        echo "IS_TEST_RUN = $IS_TEST_RUN"
        echo "=========================================================="
        ls -la
    - sleep 60          # add sleep for delay for run time
    - exit 1            # add exit 1 for creat manual error and fail pipline
